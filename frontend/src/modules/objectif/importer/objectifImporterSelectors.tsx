import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const objectifImporterSelectors = importerSelectors(
  'objectif.importer',
);

export default objectifImporterSelectors;
