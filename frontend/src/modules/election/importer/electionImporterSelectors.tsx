import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const electionImporterSelectors = importerSelectors(
  'election.importer',
);

export default electionImporterSelectors;
