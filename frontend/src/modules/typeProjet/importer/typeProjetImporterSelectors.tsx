import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const typeProjetImporterSelectors = importerSelectors(
  'typeProjet.importer',
);

export default typeProjetImporterSelectors;
