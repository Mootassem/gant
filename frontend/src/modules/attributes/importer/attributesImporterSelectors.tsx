import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const attributesImporterSelectors = importerSelectors(
  'attributes.importer',
);

export default attributesImporterSelectors;
